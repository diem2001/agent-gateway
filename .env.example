# Server
PORT=3001

# Auth — comma-separated label:key pairs
API_KEYS=default:changeme

# Logging
LOG_LEVEL=info

# Anthropic (for agent queries)
ANTHROPIC_API_KEY=sk-ant-...

# Sessions
SESSION_IDLE_TIMEOUT_MS=0
SESSION_PERSIST_PATH=./data/sessions.json
